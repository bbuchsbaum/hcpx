% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neuroim2_load.R
\name{load_asset}
\alias{load_asset}
\title{Load a single asset as a neuroim2 object}
\usage{
load_asset(h, asset_id, mode = c("normal", "mmap", "filebacked", "sparse"))
}
\arguments{
\item{h}{hcpx handle}

\item{asset_id}{Asset identifier (from assets table)}

\item{mode}{Loading mode: "normal" (in-memory), "mmap" (memory-mapped),
"filebacked" (disk-backed), or "sparse" (brain-masked only).
Default is "normal", auto-upgraded to "mmap" for files > 2GB.}
}
\value{
neuroim2 object based on file type:
\itemize{
\item CIFTI dtseries/dscalar -> DenseNeuroVec or SparseNeuroVec
\item CIFTI dlabel -> ClusteredNeuroVol
\item NIfTI 3D -> DenseNeuroVol
\item NIfTI 4D -> DenseNeuroVec
\item Confounds (.tsv) -> data.frame
\item EVs (.txt) -> data.frame
}
}
\description{
Loads a downloaded HCP asset and returns the appropriate neuroim2 object
based on file type. Requires the asset to be downloaded first (present in
the cache ledger).
}
\examples{
\dontrun{
h <- hcpx_ya()
vec <- load_asset(h, "asset_id_here")
}
}
\seealso{
\code{\link[=load_run]{load_run()}}, \code{\link[=load_task]{load_task()}}, \code{\link[=download]{download()}}
}
