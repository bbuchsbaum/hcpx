% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan.R
\name{plan_download}
\alias{plan_download}
\alias{print.hcpx_plan}
\title{Create a download plan from an assets table}
\usage{
plan_download(x, name = NULL, dry_run = TRUE)

\method{print}{hcpx_plan}(x, ...)
}
\arguments{
\item{x}{hcpx_plan object}

\item{name}{Optional name for the plan (for labeling)}

\item{dry_run}{If TRUE (default), skip HEAD requests for unknown sizes}

\item{...}{Additional arguments (ignored)}
}
\value{
An object of class \code{hcpx_plan}

Invisible x
}
\description{
Freezes a selection of assets into a portable download plan (manifest).
The plan tracks what files are needed, which are already cached, and
provides size estimates.
}
\examples{
\dontrun{
h <- hcpx_ya()

# Create plan from assets
plan <- subjects(h, gender == "F") |>
  tasks("WM") |>
  assets(bundle = "tfmri_cifti_min") |>
  plan_download(name = "wm_female")

# View the plan
plan

# Download it
download(plan)
}
}
\seealso{
\code{\link[=download]{download()}}, \code{\link[=materialize]{materialize()}}, \code{\link[=assets]{assets()}}, \code{\link[=write_manifest]{write_manifest()}}, \code{\link[=read_manifest]{read_manifest()}}
}
