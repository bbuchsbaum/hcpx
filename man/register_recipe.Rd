% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derived.R
\name{register_recipe}
\alias{register_recipe}
\title{Register a derivation recipe}
\usage{
register_recipe(
  name,
  description,
  input = c("assets", "tasks"),
  compute,
  requires_bundle = NULL,
  output_ext = "rds"
)
}
\arguments{
\item{name}{Recipe name (must be unique)}

\item{description}{Human-readable description}

\item{input}{One of "assets" (single asset) or "tasks" (all assets for a task)}

\item{compute}{Compute function that takes (paths, ...) and returns result}

\item{requires_bundle}{Bundle required for tasks-based recipes (optional)}

\item{output_ext}{Output file extension (default "rds")}
}
\value{
Invisible NULL
}
\description{
Adds a recipe to the registry that defines how to compute derived
products from HCP assets or task runs.
}
\examples{
\dontrun{
# Register a simple parcellation recipe
register_recipe(
  name = "parcellate_schaefer400",
  description = "Parcellate CIFTI with Schaefer 400 atlas",
  input = "assets",
  compute = function(paths, atlas_path) {
    # Your parcellation code here
    cifti <- read_cifti(paths[1])
    parcellate(cifti, atlas_path)
  },
  output_ext = "rds"
)
}
}
\seealso{
\code{\link[=derive]{derive()}}, \code{\link[=recipes]{recipes()}}, \code{\link[=unregister_recipe]{unregister_recipe()}}, \code{\link[=materialize]{materialize()}}
}
