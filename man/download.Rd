% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{download}
\alias{download}
\title{Download a plan (materialize)}
\usage{
download(
  plan,
  parallel = TRUE,
  workers = .DEFAULT_DOWNLOAD_WORKERS,
  resume = TRUE,
  progress = TRUE
)
}
\arguments{
\item{plan}{hcpx_plan object}

\item{parallel}{Whether to use concurrent downloads (default TRUE)}

\item{workers}{Number of concurrent transfers (default 8)}

\item{resume}{Resume partial downloads if possible (default TRUE)}

\item{progress}{Show progress bar (default TRUE)}
}
\value{
Tibble with columns: asset_id, local_path, status (cached/downloaded/failed)
}
\description{
Executes a download plan, fetching files that are not already cached.
Updates the cache ledger and returns paths to all files.
}
\examples{
\dontrun{
h <- hcpx_ya()

# Create and execute plan
paths <- subjects(h, gender == "F") |>
  tasks("WM") |>
  assets(bundle = "tfmri_cifti_min") |>
  plan_download() |>
  download()

# Or use materialize() alias
paths <- plan |> materialize()
}
}
\seealso{
\code{\link[=plan_download]{plan_download()}}, \code{\link[=materialize]{materialize()}}, \code{\link[=cache_status]{cache_status()}}, \code{\link[=load_asset]{load_asset()}}
}
