% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neuroim2_utils.R
\name{extract_series}
\alias{extract_series}
\title{Extract time series at MNI coordinates}
\usage{
extract_series(vec, coords)
}
\arguments{
\item{vec}{NeuroVec from \code{\link[=load_asset]{load_asset()}} or \code{\link[=load_run]{load_run()}}}

\item{coords}{Either:
\itemize{
\item A numeric vector of length 3 (single MNI coordinate)
\item A matrix with 3 columns (multiple MNI coordinates, one per row)
}}
}
\value{
If single coordinate: numeric vector (time series).
If multiple coordinates: matrix with dimensions (n_timepoints x n_coords).
}
\description{
Convenience function to extract time series from a NeuroVec at specified
MNI coordinates. Handles coordinate conversion automatically.
}
\examples{
\dontrun{
# Extract time series from left motor cortex
ts <- extract_series(run_data$dtseries, c(-38, -22, 56))

# Extract from multiple coordinates
coords <- rbind(
  c(-38, -22, 56),  # Left motor
  c(38, -22, 56)    # Right motor
)
ts_matrix <- extract_series(run_data$dtseries, coords)
}

}
