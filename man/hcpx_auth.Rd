% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{hcpx_auth}
\alias{hcpx_auth}
\title{Authenticate for a backend using secure storage}
\usage{
hcpx_auth(h, aws_profile = NULL, keyring_service = "hcpx", interactive = FALSE)
}
\arguments{
\item{h}{hcpx handle}

\item{aws_profile}{AWS profile name (optional). If provided, uses this
profile from ~/.aws/credentials}

\item{keyring_service}{Keyring service name for storing credentials
(default: "hcpx")}

\item{interactive}{If TRUE, prompt for missing credentials}
}
\value{
Modified hcpx handle (invisibly)
}
\description{
Configures authentication for the hcpx handle. Supports AWS profiles,
keyring-based credential storage, and interactive prompts.
}
\examples{
\dontrun{
h <- hcpx_ya()

# Use AWS profile
hcpx_auth(h, aws_profile = "hcp")

# Use keyring (will prompt if credentials not stored)
hcpx_auth(h, interactive = TRUE)

# Store credentials in keyring first
hcpx_keyring_set("my_access_key", "my_secret_key")
hcpx_auth(h)
}
}
\seealso{
\code{\link[=hcpx_auth_status]{hcpx_auth_status()}}, \code{\link[=hcpx_keyring_set]{hcpx_keyring_set()}}, \code{\link[=hcpx_keyring_clear]{hcpx_keyring_clear()}}
}
