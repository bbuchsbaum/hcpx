% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neuroim2_load.R
\name{load_task}
\alias{load_task}
\title{Load assets for multiple runs, optionally concatenated}
\usage{
load_task(x, bundle = "tfmri_cifti_min", concat = FALSE, mode = "normal")
}
\arguments{
\item{x}{tasks table (from \code{\link[=tasks]{tasks()}})}

\item{bundle}{Bundle name determining which files to load}

\item{concat}{If TRUE, concatenate time series across runs. Returns
a NeuroVecSeq or single concatenated NeuroVec.}

\item{mode}{Loading mode for neuroimaging files. See \code{\link[=load_asset]{load_asset()}}.}
}
\value{
If concat=FALSE, list of run data (each element from \code{\link[=load_run]{load_run()}}).
If concat=TRUE, single list with concatenated $dtseries and combined
$confounds and $evs.
}
\description{
Loads data for multiple task runs from a tasks table, with optional
concatenation of time series across runs.
}
\examples{
\dontrun{
h <- hcpx_ya()
runs <- tasks(h, task == "WM") |> load_task()
}
}
\seealso{
\code{\link[=load_run]{load_run()}}, \code{\link[=load_asset]{load_asset()}}, \code{\link[=tasks]{tasks()}}, \code{\link[=concat_vecs]{concat_vecs()}}
}
