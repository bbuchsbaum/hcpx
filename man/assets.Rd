% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assets.R
\name{assets}
\alias{assets}
\title{Query assets}
\usage{
assets(x, bundle = NULL, ...)
}
\arguments{
\item{x}{hcpx handle, subjects table, or tasks table}

\item{bundle}{Named bundle (recommended). See \code{bundles()} for available options.}

\item{...}{Additional filters (e.g., file_type == "dtseries", kind == "tfmri")}
}
\value{
A lazy table with class \code{hcpx_tbl} and kind "assets"
}
\description{
Returns a lazy table of assets from the catalog. Can be filtered by
bundle (recommended) or individual criteria. Supports chaining from
subjects or tasks tables.
}
\examples{
\dontrun{
h <- hcpx_ya()

# All assets
assets(h)

# Use a bundle (recommended)
assets(h, bundle = "tfmri_cifti_min")

# Chain from subjects/tasks
subjects(h, gender == "F") |>
  tasks("WM") |>
  assets(bundle = "tfmri_cifti_min")

# Custom filters
assets(h, kind == "tfmri", file_type == "ev")
}
}
\seealso{
\code{\link[=subjects]{subjects()}}, \code{\link[=tasks]{tasks()}}, \code{\link[=bundles]{bundles()}}, \code{\link[=plan_download]{plan_download()}}, \code{\link[=load_asset]{load_asset()}}
}
