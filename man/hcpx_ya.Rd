% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hcpx.R
\name{hcpx_ya}
\alias{hcpx_ya}
\alias{print.hcpx}
\alias{print.hcpx_tbl}
\title{Create an HCP-YA catalog handle}
\usage{
hcpx_ya(
  release = "HCP_1200",
  backend = c("aws", "rest", "balsa", "local"),
  cache = hcpx_cache_default(),
  engine = c("auto", "duckdb", "sqlite"),
  catalog_version = "seed",
  local_root = NULL,
  balsa_ascp = NULL,
  balsa_key = NULL,
  balsa_host = NULL,
  balsa_user = NULL,
  balsa_port = 33001L,
  balsa_rate = "300m"
)

\method{print}{hcpx}(x, ...)

\method{print}{hcpx_tbl}(x, ...)
}
\arguments{
\item{release}{Character. Default "HCP_1200".}

\item{backend}{One of "aws", "rest", "balsa", "local".}

\item{cache}{Path to cache root directory. Defaults to user cache directory.}

\item{engine}{One of "duckdb", "sqlite", "auto". Auto prefers DuckDB.}

\item{catalog_version}{One of "seed" (demo data), "latest", or a specific version.}

\item{local_root}{For local backend: path to HCP data mirror.}

\item{balsa_ascp}{For BALSA backend: path to \code{ascp} executable (optional).}

\item{balsa_key}{For BALSA backend: path to Aspera private key file (optional).}

\item{balsa_host}{For BALSA backend: Aspera host (optional; required if passing bare remote paths).}

\item{balsa_user}{For BALSA backend: Aspera username (optional; required if passing bare remote paths).}

\item{balsa_port}{For BALSA backend: Aspera port (default 33001).}

\item{balsa_rate}{For BALSA backend: rate limit passed to \code{ascp -l} (default "300m").}

\item{x}{hcpx_tbl object}

\item{...}{Additional arguments (passed to NextMethod)}
}
\value{
An object of class \code{hcpx}.

Invisible x

Invisible x
}
\description{
The main entry point for hcpx. Creates a handle to the HCP catalog with
configured backend, cache, and database engine.
}
\examples{
\dontrun{
# Initialize with defaults (AWS backend, DuckDB, demo data)
h <- hcpx_ya()

# Use local backend with existing HCP data
h <- hcpx_ya(backend = "local", local_root = "/data/HCP")

# Force SQLite if DuckDB isn't available
h <- hcpx_ya(engine = "sqlite")
}
}
\seealso{
\code{\link[=subjects]{subjects()}}, \code{\link[=tasks]{tasks()}}, \code{\link[=assets]{assets()}}, \code{\link[=overview]{overview()}}, \code{\link[=hcpx_cache]{hcpx_cache()}}, \code{\link[=hcpx_close]{hcpx_close()}}
}
