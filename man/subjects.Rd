% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subjects.R
\name{subjects}
\alias{subjects}
\title{Query subjects}
\usage{
subjects(x, ...)
}
\arguments{
\item{x}{hcpx handle or hcpx_tbl}

\item{...}{dplyr-like filter expressions (e.g., gender == "F", age_range == "26-30")}
}
\value{
A lazy table with class \code{hcpx_tbl} and kind "subjects"
}
\description{
Returns a lazy table of subjects from the catalog. Supports dplyr-style
filtering using NSE (non-standard evaluation).
}
\examples{
\dontrun{
h <- hcpx_ya()

# All subjects
subjects(h)

# Filter by gender
subjects(h, gender == "F")

# Multiple filters
subjects(h, gender == "M", age_range == "26-30")
}
}
\seealso{
\code{\link[=tasks]{tasks()}}, \code{\link[=assets]{assets()}}, \code{\link[=overview]{overview()}}, \code{\link[=hcpx_ya]{hcpx_ya()}}
}
